<style>
    table {
        border-collapse: collapse;
        font-size: 14px;
        width: 100%;
    }

    table th {
        color: var(--terms-module-gray);
        font-weight: bold;
    }

    table tbody tr td {
        min-height: 64px;
    }

    .terms-module-admin-edition-terms-name {
        color: var(--terms-module-dark);
        font-size: 24px;
        font-weight: bold;
        line-height: 32px;
        padding-bottom: 45px;
        padding-top: 20px;
        text-align: center;
    }

    .terms-module-admin-terms-card {
        align-items: center;
        border-radius: var(--terms-module-button-border-radius);
        border: 1px solid var(--terms-module-gray);
        box-shadow: none;
        color: var(--terms-module-dark);
        display: flex;
        font-family: var(--terms-module-admin-font-family);
        font-size: 16px;
        justify-content: space-between;
        line-height: 24px;
        outline: none;
        padding: 14px 25px;
        text-decoration: none !important;
    }

    .terms-module-admin-terms-card * {
        box-shadow: none;
        color: var(--terms-module-dark);
        outline: none;
        text-decoration: none !important;
    }

    .terms-module-admin-terms-card + .terms-module-admin-terms-card {
        margin-top: 15px;
    }

    .terms-module-admin-terms-card:hover {
        box-shadow: 0 0 4px 0 rgba(0, 0, 0, 0.3);
        cursor: pointer;
    }

    .terms-module-admin-terms-card-status {
        align-items: center;
        display: flex;
        justify-content: flex-end;
        min-width: 50px;
        width: 50px;
    }
</style>
