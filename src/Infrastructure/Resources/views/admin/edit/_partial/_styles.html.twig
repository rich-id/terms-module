<style>
    .terms-module-admin-edition-terms-name {
        color: var(--terms-module-dark);
        font-size: 24px;
        font-weight: bold;
        line-height: 32px;
        padding-bottom: 45px;
        padding-top: 20px;
        text-align: center;
    }

    .terms-module-admin-edition-actions {
        padding-top: 35px;
        text-align: center;
    }

    .terms-module-admin-edition-title-block {
        flex-direction: column;
    }

    @media(min-width: 1024px) {
        .terms-module-admin-edition-title-block {
            flex-direction: row;
        }
    }

    .terms-module-admin-edition-title {
        box-sizing: border-box;
        flex: 10;
        width: 100%;
    }

    .terms-module-admin-edition-publication-date {
        width: auto;
        max-width: 164px;
    }

    .terms-module-admin-row {
        margin: 0 -15px;
        display: flex;
    }

    .terms-module-admin-col {
        padding: 0 15px;
    }

    .terms-version-tag {
        align-items: center;
        background: var(--terms-module-version-tag-background);
        border-radius: 4px;
        box-sizing: border-box;
        color: var(--terms-module-white);
        display: inline-flex;
        font-size: 16px;
        height: 32px;
        margin-bottom: 8px;
        padding: 5px 7px;
        margin-right: 15px;
    }

    .terms-version-tag.enabled {
        background: var(--terms-module-version-tag-active-background);
    }

    .terms-version-tag i {
        padding-right: 8px;
    }

    .terms-module-admin-edition-add-version-container {
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        padding: 18px 0 20px 0;
    }

    @media(min-width: 768px) {
        .terms-module-admin-edition-add-version-container {
            flex-direction: row;
        }
    }

    .terms-module-admin-edition-version-management {
        white-space: nowrap;
    }

    .terms-module-admin-edition-version-management button+button {
        margin-left: 15px;
    }
</style>
