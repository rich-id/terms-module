<style>
    .terms-module-sign-header {
        background: var(--terms-module-tertiary);
        color: var(--terms-module-white);
        font-family: var(--terms-module-font-family);
        font-size: 34px;
        font-weight: bold;
    }

    .terms-module-sign-header .terms-module-container {
        padding-top: 10px;
        padding-bottom: 10px;
    }

    .terms-module-sign-body {
        padding: 60px 0 25px 0;
    }

    .terms-module-sign-content {
        background: var(--terms-module-white);
        border: 1px solid var(--terms-module-gray);
        overflow: hidden;
        padding: 30px 20px;
        position: relative;
    }

    .terms-module-ribon {
        background: var(--terms-module-secondary);
        color: var(--terms-module-white);
        font-family: var(--terms-module-font-family);
        font-size: 12px;
        font-weight: bold;
        left: 0;
        padding: 2px 0;
        position: absolute;
        text-align: center;
        top: 0;
        width: 120px;
    }

    .terms-module-sign-content-title {
        color: var(--terms-module-tertiary);
        font-family: var(--terms-module-font-family);
        font-size: 24px;
        font-weight: 500;
        text-align: center;
        padding-bottom: 50px;
    }

    .terms-module-sign-actions {
        align-items: center;
        display: flex;
        justify-content: center;
        padding-top: 30px;
        flex-direction: column;
    }

    .terms-module-action {
        display: block;
        width: 100%;
    }

    @media(min-width: 768px) {
        .terms-module-action {
            display: initial;
            width: auto;
        }

        .terms-module-sign-actions {
            flex-direction: row;
        }
    }

    @media(min-width: 1024px) {
        .terms-module-ribon {
            left: -30px;
            top: 35px;
            transform: rotate(-45deg);
            width: 150px;
        }

        .terms-module-sign-content-title {
            padding-left: 110px;
            padding-right: 110px;
        }
    }
</style>
